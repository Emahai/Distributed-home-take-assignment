seed: 42
device: cpu

dataset:
  name: cifar10
  root: ./data
  num_workers: 4

train:
  epochs: 5
  batch_size: 128
  lr: 0.01
  momentum: 0.9
  weight_decay: 0.0005
  log_every: 50

threads:
  num_threads: 64
  interop_threads: 1

output:
  dir: ./results
  run_name: base
